# Ignore node_modules for the backend
backend/node_modules/

# Keep existing frontend ignores if any by convention,
# though a root .gitignore might be better for global rules.
# For now, just ensuring backend/node_modules is covered.
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/build/

# General ignores
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # if you're not using it as a lockfile
*.env
.env.local
.env.development.local
.env.test.local
.env.production.local
